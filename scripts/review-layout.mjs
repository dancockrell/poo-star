import {chromium} from '@playwright/test';
const b=await chromium.launch({channel:'chrome',headless:true});const p=await b.newPage();await p.goto('http://127.0.0.1:8791');for(const width of [320,390,600,760,761,900,1440,2560]){await p.setViewportSize({width,height:900});await p.evaluate(()=>document.fonts.ready);const issue=await p.evaluate(()=>{const f=document.querySelector('footer').getBoundingClientRect(),c=document.querySelector('.controls').getBoundingClientRect(),s=[...document.querySelectorAll('footer span')].map(e=>e.getBoundingClientRect());return document.documentElement.scrollWidth>innerWidth||f.top<c.bottom||s[0].right>s[1].left;});if(issue)throw Error(`Footer overlap/overflow at ${width}`);}await p.setViewportSize({width:1440,height:1030});await p.locator('.feature-board').screenshot({path:'docs/chalkboard.png'});await b.close();console.log('Footer passed eight widths, 320–2560px');
